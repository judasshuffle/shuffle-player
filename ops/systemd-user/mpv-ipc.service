[Unit]
Description=MPV idle daemon with IPC socket for Jukebox
After=default.target

[Service]
Type=simple
ExecStart=/usr/bin/mpv --no-video --idle=yes --input-ipc-server=/tmp/radio_mpv.sock
Restart=on-failure
RestartSec=1
KillMode=process

[Install]
WantedBy=default.target
